import './styles/index.css';

export { default as BasePill } from './components/BasePill.vue';

export type {
    Size,
    Color,
    HasSize,
    HasDisabled,
    HasColor,
    BaseComponentProps,
} from './types';
